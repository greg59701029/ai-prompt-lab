# Add before and after prompt diff view

Help users compare prompt drafts after editing a template, importing a preset, or changing constraints.

## Acceptance Criteria

- Users can capture the current prompt as a baseline
- UI shows changed lines between baseline and current prompt
- Diff view works without external dependencies
- E2E test covers baseline capture and changed prompt output

## Suggested Labels

`enhancement`, `ui`
